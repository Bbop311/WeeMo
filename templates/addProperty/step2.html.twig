{% extends 'base.html.twig' %}

{% block body %}
<h1>Step 2: Additional Information</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ path('app_add_property_step1') }}" class="btn btn-secondary">Back</a>
    <button type="submit" class="btn btn-primary">Next</button>
{{ form_end(form) }}
{% endblock %}
